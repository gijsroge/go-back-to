{
	"name": "go-back-to",
	"version": "1.1.0",
	"description": "A lightweight TypeScript utility that navigates back to a specific route in browser history using the Navigation API, preserving scroll position",
	"type": "module",
	"main": "./dist/index.js",
	"module": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		"./package.json": "./package.json",
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js",
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsdown src/index.ts && pnpm run build:fix-types",
		"build:fix-types": "node -e \"const fs=require('fs');const files=fs.readdirSync('dist');const dts=files.find(f=>f.endsWith('.d.ts')&&f.startsWith('index-'));if(dts)fs.copyFileSync('dist/'+dts,'dist/index.d.ts');\"",
		"prepublishOnly": "pnpm run build && pnpm run test:publint",
		"test:unit": "vitest run --passWithNoTests",
		"test:cov": "vitest run --coverage",
		"test:types": "tsc",
		"test:publint": "publint --strict",
		"test:exports": "attw --pack ."
	},
	"author": "",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/gijsroge/go-back-to.git"
	},
	"bugs": {
		"url": "https://github.com/gijsroge/go-back-to/issues"
	},
	"files": ["dist"],
	"homepage": "https://github.com/gijsroge/go-back-to#readme",
	"publishConfig": {
		"access": "public"
	},
	"devDependencies": {
		"@arethetypeswrong/cli": "^0.17.4",
		"@changesets/cli": "^2.29.0",
		"@types/dom-navigation": "^1.0.6",
		"@types/node": "22.14.1",
		"@vitest/coverage-v8": "^3.1.1",
		"happy-dom": "^17.4.4",
		"tsdown": "^0.15.6",
		"typescript": "^5.8.3",
		"vitest": "^3.1.1"
	}
}
